1. What are the different data types present in js?
- Primitive types > String - Written with quotes. A string can be represented using a single or a double quotes.
EX: var str="Fumika"
    var str2=`Fumika`

-Number > can be written with or without decimals

-Boolean > it represents a logical entity and can ave only two values: truw or false. Booleans are generally used for conditional testing.

-Undefined > When a variable is declared but no assigned, it has the value of undefined and it's type is also undefined.

-